<Window x:Class="HotelApp.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelApp.Desktop"
        mc:Ignorable="d" FontSize="20" FontFamily="Segoe UI"
        Title="Search Bookings" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>


        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" FontSize="28">Search For Rooms</TextBlock>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" >
            <!--<TextBlock Margin="5" Text="First Name:" Grid.Row="1" Grid.Column="0"/>-->
            <TextBox MinWidth="200" Name="lastNameTxt" Margin="5" ></TextBox>
            <Button x:Name="searchForGuest"  Margin="5" Padding="5" Click="searchForGuest_Click">Search</Button>
        </StackPanel>


        <ListBox x:Name="bookingsList" ItemsSource="{Binding}"  Grid.Row="3" Grid.ColumnSpan="2" Margin="5 10 5 10" ScrollViewer.VerticalScrollBarVisibility="Auto" MaxHeight="300" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>

                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Row="0">
                            <TextBlock FontWeight="Bold" Margin="5">Reservation:</TextBlock>
                            <TextBlock Margin="5" Text="{Binding FirstName}" />
                            <TextBlock Margin="5" Text="{Binding LastName}" />
                        </StackPanel>
                            <Button Margin="5" Padding="10" Grid.Row="0" Grid.Column="1" Grid.RowSpan="4" VerticalAlignment="Center">
                                Check In
                            </Button>
                        <StackPanel Orientation="Horizontal" Grid.Row="1">
                            <TextBlock FontWeight="Bold" Margin="5">Room:</TextBlock>
                            <TextBlock Margin="5" Text="{Binding Title}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="2">
                            <TextBlock FontWeight="Bold" Margin="5">Room Number:</TextBlock>
                            <TextBlock Margin="5" Text="{Binding RoomNumber}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="3">
                            <TextBlock FontWeight="Bold" Margin="5">TotalCost:</TextBlock>
                            <TextBlock Margin="5" Text="{Binding TotalCost, StringFormat='c'}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>

            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
