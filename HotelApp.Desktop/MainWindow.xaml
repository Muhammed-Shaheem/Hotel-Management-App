<Window x:Class="HotelApp.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelApp.Desktop"
        mc:Ignorable="d" FontSize="20" FontFamily="Segoe UI"
        Title="Search Bookings" Height="450" Width="800">
    <Grid Margin="20" Background="#f5f5f5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" FontSize="32" FontWeight="Bold" Foreground="#333" Margin="0 0 0 20">
        Search For Rooms
        </TextBlock>

        <!-- Search Bar -->
        <TextBlock Grid.Row="1" Margin="5"  FontFamily="Consolas">Last Name</TextBlock>

        <StackPanel  Orientation="Horizontal" Grid.Row="2" Margin="5">
            <TextBox Name="lastNameTxt" MinWidth="250" Padding="8" Margin="0 0 10 0"
                 Background="White" BorderBrush="#ccc" BorderThickness="1"
                 FontSize="16" />
            <Button x:Name="searchForGuest" Padding="8 0" Width="120"
                Background="#007ACC" Foreground="White" FontWeight="Bold"
                BorderThickness="0" Click="searchForGuest_Click">
                Search
            </Button>
        </StackPanel>

        <!-- Bookings List -->
        <ListBox x:Name="bookingsList" ItemsSource="{Binding}" Grid.Row="2"
             ScrollViewer.VerticalScrollBarVisibility="Auto" MaxHeight="400"
             Background="Transparent" BorderThickness="0" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="10" Margin="0 0 0 15" Padding="15"
                        BorderBrush="#ddd" BorderThickness="1" 
                        Effect="{DynamicResource DropShadowEffect}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center" >
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontWeight="Bold" Foreground="#555">Reservation:</TextBlock>
                                    <TextBlock Text="{Binding FirstName}" />
                                    <TextBlock Text="{Binding LastName}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontWeight="Bold" Foreground="#555">Room:</TextBlock>
                                    <TextBlock Text="{Binding Title}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontWeight="Bold" Foreground="#555">Room Number:</TextBlock>
                                    <TextBlock Text="{Binding RoomNumber}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontWeight="Bold" Foreground="#555">Total Cost:</TextBlock>
                                    <TextBlock Text="{Binding TotalCost, StringFormat='c'}" />
                                </StackPanel>
                            </StackPanel>

                            <Button x:Name="checkIn" Grid.Column="1" Padding="12 6" Width="120"
                                Background="#28a745" Foreground="White" FontWeight="Bold"
                                BorderThickness="0"  VerticalAlignment="Center"
                                Click="checkIn_Click">
                                Check In
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

</Window>
